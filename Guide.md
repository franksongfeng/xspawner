# XSpawner 指南

**XSpawner** 是一个轻量级的 Python Web 管理平台，旨在帮助开发者快速构建小型但功能完整的异步 Web 服务。它将 **Tornado** 的高性能异步能力与 **PyWebIO** 的快速 UI 组件相结合，显著简化了现代 Web 应用的开发流程，尤其适合熟悉 Python 并希望避免前后端分离复杂性的开发者。

------

## 一、核心功能

XSpawner 提供了以下关键特性：

- **🐍 全 Python 技术栈**：前端和后端均使用纯 Python 开发，无需切换语言或框架。
- **📦 类即服务（Class as a Service, CaaS）**：每个服务都被封装为一个独立的类，便于组织、复用和管理。
- **🌐 简单的生命周期管理**：通过依赖关系管理服务的创建、启动和停止等生命周期操作。
- **⚡ 异步编程**：基于 Tornado 久经考验的协程能力，提供高性能的异步处理。
- **🔒 安全增强**：原生支持 SSL/TLS 证书，保障通信安全。
- **🗄️ 内置数据库支持**：基于 Tortoise ORM，支持 SQLite、MySQL 和 PostgreSQL，并内置数据库备份与清理功能。

------

## 二、用途与适用场景

XSpawner 适合以下场景：

- **快速原型开发**：需要快速搭建一个带有 UI 的异步 Web 服务，验证想法。
- **小型管理平台**：构建轻量级的后台管理工具或内部服务。
- **微服务/服务编排**：利用“类即服务”和生命周期管理能力，组织多个独立服务。
- **异步任务处理**：需要利用 Tornado 协程处理高并发或异步任务的场景。

------

## 三、结构

XSpawner 的项目结构清晰，采用模块化设计：

- **核心模块**（`xspawner.py`、`service.py`、`orm.py`、`constants.py`）位于主包根目录。
- **插件系统**（`plugins/`）支持 `spawner` 和 `supervisor` 两个核心插件，每个插件可独立包含测试和静态资源。
- **工具库**（`utilities/`）提供日志、客户端、消息等通用功能。
- **示例代码**（`demo/`）覆盖多种使用场景，便于快速上手。
- **根目录脚本**（`init`、`start`、`stop`、`backup`、`drop`）提供服务命令行管理入口。

主程序包内部按功能划分了多个子目录和模块。

| 名称             | 类型 | 说明                       |
| :--------------- | :--- | :------------------------- |
| `demo/`          | 目录 | 示例代码目录               |
| `plugins/`       | 目录 | 插件目录，包含核心插件实现 |
| `static/`        | 目录 | 静态文件目录               |
| `utilities/`     | 目录 | 工具函数目录               |
| `__init__.py`    | 文件 | 包初始化文件               |
| `__main__.py`    | 文件 | 程序入口文件               |
| `__version__.py` | 文件 | 版本信息文件               |
| `constants.py`   | 文件 | 常量定义文件               |
| `orm.py`         | 文件 | 对象关系映射文件           |
| `service.py`     | 文件 | 服务管理核心模块           |
| `xspawner.py`    | 文件 | 异步Web请求的处理          |
| `spawner.py`     | 文件 | 服务的生命周期管理         |

------

## 四、项目信息

- **许可证**：Apache-2.0
- **作者**：Song Feng
- **联系方式**：43217183@qq.com
- **仓库地址**：https://github.com/franksongfeng/xspawner

------

